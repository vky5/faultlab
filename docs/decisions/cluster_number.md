    we have two options, either go for multi cluster setup,
    each cluster managing their own nodes. and the control plane managing each cluster or we can go for the 
    single cluster setup. For now we are going with single cluster setup.

    no changed my mind. just need cluster_id. Will be setting that up as well.

    But in my decision each node can belong to one cluster only. No single node multi cluster setup for now.

