# Transport layer
transport layer is the part of node binary and it is responsible for the communication between nodes.

Earlier we have designed it like this 
Node A ------ Node B

but in later designs we are switching to this 
Node A ------ Transport ----- Node B

and this is where we will later introduce the NetworkSim through which we will inject the faults 

